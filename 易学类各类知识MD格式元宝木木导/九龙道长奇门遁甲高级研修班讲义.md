# 奇门遁甲高级研修班讲义（1~22 讲）

> 源文件：`奇门遁甲高级研修班讲义1~22.doc`（老式 Word 文档，约 18 MB）

## ⚠️ 转换说明

本讲义原始文件为 **WPS 生成的扫描件型 `.doc`（OLE2 格式）**，文档内容以**嵌入图片**为主体，**没有可提取的文字层**。

经多种工具尝试，均**无法可靠还原正文**：

| 方案 | 结果 |
|---|---|
| LibreOffice 转 `.docx` | 报错 *"Please verify input parameters"*（Code:13，通用 I/O 错误） |
| LibreOffice 导出 `.txt` | 输出文件约 80% 为空字节，非有效文本 |
| `olefile` 解析 Word 文档流 | 文本采用 WPS 私有编码，无法解码 |
| `antiword` / `catdoc` | 当前环境不可用 |
| GBK / GB18030 / UTF-16 解码 | 严格解码均失败 |

**为保证内容真实性，本文件不臆造讲义正文。**

## 已知结构（来自讲义目录框架）

根据文件标题，讲义共 **22 讲**，主题为奇门遁甲高级研修，典型内容应涵盖：

1. 奇门遁甲总论与参数体系
2. 天干、地支、八卦九宫
3. 九星、八门、八神
4. 八神、八门、九星组合
5. 转盘与飞盘排局法
6. 用神与断局思路
7. 各类预测专题（健康、婚恋、事业、财运、风水等）
8. ……（第 8~22 讲）

> 注：以上为根据标题的合理推断，**非原文内容**，仅供参考。

## 如何获得完整文本

如需将本讲义完整转为 Markdown，推荐以下任一方式：

1. **桌面环境另存**：在装有 Microsoft Word 或 WPS 的电脑打开原 `.doc`，
   选择「另存为 → `.docx`」或「另存为 → `.txt`（UTF-8）」，再交由转换工具处理。
2. **OCR 识别**：对扫描页使用 OCR 工具（如白描、ABBYY FineReader、
   PaddleOCR、Tesseract 中文包）批量识别，再将文本整理为 Markdown。
3. **重新提供源文件**：若能提供由 Word/WPS「另存为 .docx」后的版本，
   或直接提供已导出的 `.txt`，可立即重新转换。

---

*本说明文件由自动转换流程生成，旨在如实记录转换限制，避免以乱码冒充正文。*
