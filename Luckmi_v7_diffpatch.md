# LUCKMI v7 补丁（可粘贴版）

> 用法：把下面三块分别粘贴到 `Luckmi_整体规划策略与执行步骤_v7.md` 对应位置。
> - **[新增]** → 在原文档末尾追加
> - **[替换]** → 找到原段落，整段替换
> - **[插入]** → 在指定锚点后插入

---

## 块 A：[替换] §3 ADR-2（锁定古籍四体书 + 开源优先）

**锚点**：§3「关键架构决策（ADR）」→ ADR-2 命理事实源唯一

**原内容**：
```
- ADR-2 命理事实源唯一：所有干支/十神/神煞/五行/格局断语只能来自 `src/lib/bazi|shensha` 确定性引擎（带古籍出处：《渊海子平》《子平真诠》《穷通宝鉴》《三命通会》）。LLM 仅做双语白话转写，转写结果不入命理事实层。派别参数唯一来源 `config.ts`。
```

**替换为**：
```
- ADR-2 命理事实源唯一 + 开源优先：
  - 所有干支/十神/神煞/五行/格局/喜用神/流年流月断语，只能来自 `src/lib/bazi|shensha` 确定性引擎 + 四部古籍出处（《渊海子平》《子平真诠》《穷通宝鉴》《三命通会》）。LLM 仅做双语白话转写，转写结果不入命理事实层。
  - **开源优先原则（最高优先级）**：排盘、起名、评分、校验各环节，必须优先复用已锁定的开源引擎/数据，禁止 LLM 自行推算或自造算法：
    · 排盘：lunar-javascript（禁止手写干支/节气/翻子时）
    · 流年/流月/流日：bazi-engine（禁止手写干支递推）
    · 字库五行/康熙笔画：Fate gua.data + lunar 藏干
    · 英文名映射：Chinese-Names-Corpus（English_Names_Corpus）
    · 拼音/音译：pinyin-pro 或 node-pinyin（禁止手写拼音映射表）
    · 音近相似度：string-similarity 或 jaro-winkler
    · 数理吉凶：既定 81 数理表（仅查表，不自造）
  - LLM 允许参与：音译/意译通道的语义匹配、候选名白话转写、报告双语翻译。LLM **禁止**参与：排盘、五行打分、喜用神判定、格局识别、名字本体生成。
  - 派别参数唯一来源 `config.ts`；口径定义见「神算子 Agent Prompt §📚③」。
```

---

## 块 B：[插入] §3.5 神算子 Agent 协作协议

**锚点**：放在 §3 末尾（ADR-6 之后）、§4 之前

**插入内容**：
```markdown
## 3.5 神算子 Agent 协作协议

本项目由 TRAE CODE 内「神算子」Agent 作为全栈开发执行者，负责从 M1 起的全栈开发。

### B.1 Agent 已内置的约束（项目级，不可违反）

- **派别参数**：`src/lib/bazi/config.ts` 为唯一来源；晚子时翻日由 lunar-javascript 处理，业务层不得二次翻日
- **神煞口径**：天干类 = 日干（日元/日柱天干）；地支类 = 年支(大) + 日支(小)，大小并存
- **天乙贵人歌诀**：锁定 `甲戊庚牛羊`，不提供另一套，不运行时切换
- **五行打分**：得令(月支)≈50% + 得地(藏干本/中/余气加权)≈30% + 得势(天干比劫印星)≈20%；≥70 强，≤39 弱
- **格局**：仅输出可确定的八正格；从格/化格/专旺/识别不全 → 不输出格局卡（连"需人工复核"都不写）
- **古籍引用**：仅限《渊海子平》《子平真诠》《穷通宝鉴》《三命通会》四体书 + bazi-engine 带出处库，须标注出处
- **开源优先**：见 ADR-2

### B.2 Agent 启动与协作流程

1. 启动时先读 `docs/Luckmi_整体规划策略与执行步骤_v7.md` 与 `src/lib/bazi/config.ts`，复述对本项目的理解（技术栈/派别口径/已完成模块/待做优先级/M1 核心约束），**待用户确认后才动手**
2. 每次改动前先读 `config.ts`，确认当前派别参数，禁止在业务代码另写一套
3. 新增模块若涉及排盘/节气/翻子时，必须调 lunar-javascript 或 bazi-engine，**禁止手写**
4. 起名候选生成优先用开源数据（Chinese-Names-Corpus / Fate gua.data / pinyin-pro），LLM 只做转写不生成名字本体
5. 小步提交，每完成一个功能点跑 `npm run test:bazi`，全绿才继续
6. 关键用例必须覆盖：节气交接日、闰月、晚子时、闰年 2/29、夏令时出生、海外(非东八区)出生、神煞(日干天乙/年支大桃花大驿马/日支小桃花小驿马)、五行占比校准、格局不输出场景
7. 报告功能先做数据结构 + snapshot 测试，再接渲染层
8. 需求模糊（起名风格/调候口径/报告详略/PDF）先问用户，不猜

### B.3 Agent 启动指令（在 TRAE CODE 对话中发送）

> @神算子 请阅读 `docs/Luckmi_整体规划策略与执行步骤_v7.md`，这是项目完整规划。当前状态：M0 已完成（八字引擎 168 测试全绿），代码在 `src/lib/bazi|shensha` 与 `src/app/components`。派别参数见 `src/lib/bazi/config.ts`。请先复述：你对本项目的理解（技术栈 / 派别口径 / 已完成模块 / 待做优先级 / M1 起名引擎的核心约束），确认无误后从 M1 开始执行。约束：起名引擎必须优先利用开源库（Chinese-Names-Corpus / Fate gua.data / pinyin-pro / bazi-engine），LLM 只做转写，不排盘、不生成名字本身、不判定喜用神。

### B.4 项目文件定位（Agent 参考）

```
luckmi/
├── .trae/
│   ├── agents/bazi-naming-dev.md   # 神算子 Agent Prompt（派别+规则+领域知识）
│   └── rules/bazi-naming.md        # 硬性规则（提交前自动校验）
├── docs/
│   └── Luckmi_整体规划策略与执行步骤_v7.md   # 本规划文档
├── src/lib/
│   ├── bazi/{config,paipan,wuxing,pattern,shishen,dayun,timeline}.ts
│   ├── shensha/{tiangan,dizhi,guaxiang}.ts
│   ├── astronomy/solarTime.ts
│   ├── naming/englishToChinese.ts
│   ├── reports/{life,year,month}.ts
│   └── llm/prompt.ts               # DeepSeek 转写 system prompt
├── src/app/api/char-meta/route.ts  # 字元数据库 API（前端禁引 >2MB 字库）
└── tests/                          # *.test.ts + snapshots/
```
```

---

## 块 C：[替换] §7 起名引擎设计（补开源库清单）

**锚点**：§7「起名引擎设计（M1 核心，本版细化重点）」

**原内容**（开头段落，含流程图）：

```
## 7. 起名引擎设计（M1 核心，本版细化重点）

（原流程图与说明……）
```

**替换为**：
```
## 7. 起名引擎设计（M1 核心，本版细化重点）

### 7.0 开源依赖锁定（必须遵守，禁止 LLM 自造算法）

| 环节 | 开源库 / 数据源 | 用途 | 禁止事项 |
|---|---|---|---|
| 字库五行/康熙笔画 | Fate gua.data + lunar 藏干 | char-meta 字段来源 | 自造五行归属（需 PR 说明依据） |
| 英文名→中文映射 | Chinese-Names-Corpus（English_Names_Corpus） | 音译/意译通道种子 | LLM 凭语感硬编英文名含义 |
| 拼音/音译 | pinyin-pro 或 node-pinyin | 汉字→拼音，声调提取 | 手写拼音映射表 |
| 音近相似度 | string-similarity 或 jaro-winkler | 声母/韵母/声调相似度阈值 | 自造相似度算法 |
| 数理吉凶 | 既定 81 数理表（静态数据） | 五格摘要 | 动态生成数理含义 |
| 喜用契合 | `wuxing.calcXiYong`（确定性引擎） | 候选过滤与评分 | LLM 参与喜用判定 |
| 候选转写/报告 | DeepSeek API 或阿里云百炼 | 白话转写、双语翻译 | 转写内容混入命理判断 |

> 上述库须在 `package.json` 中锁定为**主流、维护活跃版本**；不擅自升级大版本。

### 7.1 引擎流程（LLM 边界标注）

```
输入 {surname|englishName, gender, birth{y,m,d,h,min}, lon?, lat?, style?}
  │
  ├─① paipan() 确定性引擎（lunar-javascript + bazi-engine）→ 喜用神/忌神
  │    【LLM 禁止参与】
  │
  ├─② 候选生成双通道
  │    ├ 音译通道：英文名音节 → pinyin-pro 拼音 → string-similarity 音近汉字簇（≥阈值）
  │    │              【拼音/相似度用开源库，LLM 仅做最终语义润色】
  │    ├ 意译通道：Chinese-Names-Corpus 英文含义 → 字义匹配汉字簇
  │    │              （含《诗经》《楚辞》等典籍意象标签）
  │    └ 宝宝名通道：辈分字/定字约束 + 姓氏搭配（字形结构/声调流，用 pinyin-pro 取调）
  │
  ├─③ 回灌校验（硬过滤，一票否决）【全部走确定性规则，LLM 不参与】
  │    ├ 五行补缺：候选字五行 ∈ {喜用神} ∪ {不忌}，且不加重忌神（Fate gua.data）
  │    ├ 五格/81 数理摘要（仅既定表）
  │    ├ 谐音审查：普通话/粤语粗俗谐音黑名单
  │    ├ 避讳审查：长辈名/历史负面人物/品牌商标
  │    ├ 生僻字过滤：Unicode 常用集 + 海外输入法可打性
  │    └ 流行度标注：US SSA / 中文重名率
  │
  ├─④ 评分排序（权重配置化，锁死于 config.ts）
  │    score = 0.35*喜用契合 + 0.20*音律 + 0.15*字义 + 0.10*字形
  │          + 0.10*数理 + 0.10*独特性
  │
  └─⑤ 输出 top N（10 条 / $49 报告 5 条精解）候选 JSON（§6.1）
        【仅此步允许 DeepSeek 对候选名做白话/双语转写，转写不入事实层】
```

### 7.2 测试要求（与既有 168 测试同一门禁）

- 喜用契合：基准盘（辛金极弱，喜土金）→ 候选名五行含土/金占比 100%，无纯木火水名
- 音译/意译通道各给出确定性输出（固定随机种子）→ snapshot 测试
- 回灌校验反例：构造忌神名/谐音名/生僻字名 → 必须被过滤
- 数理摘要与既定 81 表逐项对照
- **拼音/音译库对比测试**：pinyin-pro vs node-pinyin 输出对齐抽样校验，选定一个后锁死

```
（原流程图保留，此处替换为上表 + 上述测试要求）
```
```

---

## 块 D：[新增] §3 ADR-7（Agent 工程化约束，追加到 ADR 列表末尾）

**锚点**：§3 ADR-6 之后

**新增内容**：
```markdown
- **ADR-7 Agent 工程化约束**：
  - Agent 改动须遵循「先方案 → 用户确认 → 小步提交 → 测试全绿」循环
  - 所有排盘/命理计算放服务端（`src/lib/`），前端仅渲染；字库走 `/api/char-meta`，禁止前端 import >2MB 字库 JSON
  - 每次提交前 `npm run test:bazi` 全绿 + `npx tsc --noEmit` 无类型错误
  - 报告断语一律来自 bazi-engine 带出处库；LLM 只转写不判
  - API Key 全部环境变量，禁止硬编码/提交
```

---

## 块 E：[新增] §1.1 补充 Agent 上下文文件（追加到已完成表之后）

**锚点**：§1.1 表格之后

**新增内容**：
```markdown
### 1.1b Agent 上下文（新增，供神算子启动时读取）

| 层 | 文件 | 用途 |
|---|---|---|
| 配置 | `.trae/agents/bazi-naming-dev.md` | 神算子 Agent Prompt（派别+规则+领域知识） |
| 规则 | `.trae/rules/bazi-naming.md` | 提交前自动校验的硬性规则 |
| 规划 | `docs/Luckmi_整体规划策略与执行步骤_v7.md` | 本规划文档（Agent 启动指令见 §3.5 B.3） |
| 派别 | `src/lib/bazi/config.ts` | 派别参数唯一来源（Agent 每次改动前必读） |
```

---

## 应用顺序

1. **块 A**：替换 §3 ADR-2（在「命理事实源唯一」段按上替换）
2. **块 B**：插入到 §3 末尾、§4 之前（神算子协作协议 + 启动指令）
3. **块 C**：替换 §7 开头（补开源库清单表 + LLM 边界）
4. **块 D**：追加到 §3 ADR-6 之后（ADR-7 Agent 工程化约束）
5. **块 E**：追加到 §1.1 表格之后（Agent 上下文文件表）

应用完后，在 TRAE CODE 里用神算子 Agent 发 **§3.5 B.3 的启动指令** 即可接管项目。
